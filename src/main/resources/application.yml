spring:
  profiles:
    active: default
  datasource:
    url: jdbc:mysql://${DATABASE_HOST:my-project-db-rds.cl86wcsmc863.eu-west-1.rds.amazonaws.com}:${DATABASE_PORT:3306}/${DATABASE_NAME:my_project_db}${DATABASE_OPTIONS:}
    username: ${DATABASE_USER:admin}
    password: ${DATABASE_PASSWORD:P1ngu1n0?!}
  jpa:
    hibernate:
      ddl-auto: update
#server port utile per quando si deploya su AWS
server:
  port: ${SERVER_PORT:5000}

